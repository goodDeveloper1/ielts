import 'package:flutter/material.dart';

class SpeakingQuestionsList extends StatelessWidget {
  const SpeakingQuestionsList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}